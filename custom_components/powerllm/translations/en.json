{
  "config": {
    "step": {
      "user": {
        "data": {
          "name": "Entry name"
        }
      },
      "init": {
        "data": {
          "duckduckgo_region": "DuckDuckGo Region",
          "script_exposed_only": "Only allow referencing exposed entities in scripts"
        },
        "data_description": {
          "script_exposed_only": "Disabling would allow more complicated scripts, but could affect unexposed entities"
        },
        "description": "Select both Assist and this PowerLLM API in your conversation agent to use home control together with PowerLLM tools."
      },
      "memory_prompts": {
        "title": "Memory tool prompts",
        "description": "Enter facts that the system should know about each user."
      },
      "tool_selection": {
        "title": "Tool selection",
        "description": "Select PowerLLM tools available for this API. These settings do not filter Assist tools.",
        "data": {
          "default": "Default"
        },
        "data_description": {
          "default": "Other tools not listed above"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "duckduckgo_region": "DuckDuckGo Region",
          "script_exposed_only": "Only allow referencing exposed entities in scripts"
        },
        "data_description": {
          "script_exposed_only": "Disabling would allow more complicated scripts, but could affect unexposed entities"
        },
        "description": "Select both Assist and this PowerLLM API in your conversation agent to use home control together with PowerLLM tools."
      },
      "memory_prompts": {
        "title": "Memory tool prompts",
        "description": "Enter facts that the system should know about each user."
      },
      "tool_selection": {
        "title": "Tool selection",
        "description": "Select PowerLLM tools available for this API. These settings do not filter Assist tools.",
        "data": {
          "default": "Default"
        },
        "data_description": {
          "default": "Other tools not listed above"
        }
      }
    }
  }
}
